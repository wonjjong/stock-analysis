# 변경 이력

이 문서는 여러 도메인에 걸친 작업, 플랫폼·설정·저장소 공통 변경과 중요한 도메인 변경을
날짜순으로 기록한다. 도메인의 현재 동작과 상세 계약은 `docs/domains/<도메인>/README.md`를
기준으로 한다.

## 2026-09-09

### Gemini 429 대응과 중복 분석 호출 축소

- 실제 DB 공급자 상태에서 앱 집계는 `3/100`, 쿨다운 없음이었고 등록된
  `gemini-3.7-flash`와 OpenAI 호환 Base URL이 유효함을 확인했다. 실제 키를 노출하지 않은
  최소 호출은 200 JSON 응답을 받아 인증·모델 설정 문제가 아님을 검증했다.
- Gemini OpenAI 호환 호출에 `reasoning_effort=low`를 지정해 기본 medium 사고량보다
  출력·사고 토큰 소모를 줄였다. 429 분당·토큰 한도는 1·2·4초 지수 백오프로 재시도하며,
  일일 한도가 명시된 응답은 재시도하지 않는다.
- 계속되는 429는 응답의 `retryDelay`(15~300초)를 적용하고 없으면 1분, 일일 한도는 다음
  KST 자정까지 쿨다운한다. 402와 quota 성격의 400은 기존 1시간 정책을 유지한다.
- 종목 분석의 31개 지표는 AI에 값·단위·출처·기간만 전달하며 화면 설명문과 결측 항목을
  제외했다. 동일 종목·마지막 일봉·표시 가격·채택 근거 ID의 성공 보고서는 20분 캐시한다.
  시가총액·제공 배수·거시 조회의 미세 변동은 캐시를 무효화하지 않고, 새 뉴스/공시가 채택되면
  다시 호출하며 실패 폴백은 캐시하지 않는다.
- 공급자 화면의 `오늘 사용`을 `앱 집계`로 명확히 바꾸고 내부 재시도·연결 확인이 제외되며
  실제 Gemini RPM·TPM·RPD는 AI Studio에서 확인해야 한다고 표시했다.
- DB 스키마·마이그레이션·환경변수 변경은 없다. 무료 티어 사용을 유지한다.
- 실제 AAPL 분석에서 `gemini-3.7-flash`의 Google 무료 요청 할당량 429를 재현했다. 앱의
  논리 호출은 8/100이었으므로 앱 상한이 아니라 Google 프로젝트의 별도 한도임을 확인했다.
  신규 프로젝트에서 `gemini-2.5-flash`는 종료 모델 404를 반환했고,
  `gemini-3.5-flash-lite`는 같은 키로 14,309자 종목 분석 입력과 요구 JSON을 정상 처리했다.
  무료 운용을 우선해 DB의 Google Gemini 모델과 신규 등록 프리셋을
  `gemini-3.5-flash-lite`로 변경하고 기존 429 쿨다운을 해제했다. Base URL·키는 유지했다.
- 변경된 DB 공급자로 실제 AAPL 분석을 실행해 첫 요청은 약 14초에 Gemini 200 응답을 받았고,
  같은 근거의 두 번째 요청은 약 7초에 `20분 이내 동일 근거` 캐시를 사용했다. 두 번째 요청에는
  Gemini POST가 발생하지 않았다.
- 검증: 전체 pytest **281 passed, 1 skipped**, 전체 Ruff와 `git diff --check` **통과**.
  429 분당/일일 판별, 1·2·4초 재시도, Gemini low 사고 수준, 쿨다운, AI 컨텍스트 축소,
  공급자 부가값 변동에도 유지되는 성공 결과 캐시를 회귀 테스트로 고정했다.

### 종목 지표 설명과 가정별 가치평가 추가

- 요구: 다양한 재무·기술 지표와 용어 설명, 실무에서 쓰는 가치평가 방식 제공. Gemini 공급자
  설정은 유지하고 기존 공개 데이터만 이용했다. 신규 유료 API·환경변수·패키지·마이그레이션은 없다.
- `research/technical_indicators.py`, `indicators.py`, `valuation.py`에 순수 계산을 추가했다.
  `market_data.py`는 추가 비율·연간 재무·통화·결산일을 수집하고 재무 조회 실패는 결측으로 처리한다.
  같은 기간의 대체 계정을 사용하되 서로 다른 기간이나 통화를 임의로 섞지 않는다.
- 분석실과 미리보기에 재무/가치/기술/위험 31개 지표를 제공한다. 새 `indicators.html`은
  용어·수식·출처·기간·결측 이유를 펼칠 수 있다. 기존 목표선은 `ATR 목표 참고가`로 구분했다.
- `services/stock_analysis.py`와 AI 프롬프트는 지표와 데이터 한계를 공유한다. JSON 분석에
  `indicators`, `valuationInputs`를 추가하고 기존 점수·추천·ATR 산식 계약은 유지했다.
- `POST /api/research/valuation`은 사용자 가정으로 PER/PBR/EV·EBITDA/FCFF DCF를 계산한다.
  AI·외부 조회·DB 저장 없이 재계산하고 CSRF 보호를 적용한다. 가정은 자동 입력하지 않는다.
  DCF는 5년 현금흐름과 영구가치를 할인하며 EPS/EBITDA/자본·통화 누락은 해당 방식만 비운다.
  잘못된 배수·성장률·WACC는 400, 계산 가능 범위를 벗어난 값은 null/오류로 처리한다.
- 한계: 증권사 컨센서스, 적정 배수 자동 추정, 금융업 특화 모형, 우선주·소수주주지분 조정은
  제공하지 않는다. 단순 FCF를 FCFF로 간주하지 않으며 DCF의 정상화 FCFF는 직접 입력한다.
- 문서: `docs/domains/research/README.md`에 계산식·입력 범위·응답·시점·무료 데이터 한계를 기록했다.
- 검증: 전체 pytest **273 passed, 1 skipped**, 전체 Ruff **통과**. 기술지표 27개 테스트,
  재무/가치평가·CSRF·공통 유스케이스 회귀 테스트를 추가했다. QA가 찾은 무한대 상승률과
  극소 할인율의 0 나눗셈을 수정하고 재현 테스트를 추가했다.
- 브라우저: 미리보기에서 PER·DCF 정상 계산, 잘못된 WACC 오류, 재시도 버튼 복구,
  설명 펼침·포커스, 390px 가로 넘침 없음을 확인했다. 스크린샷은 캡처 타임아웃으로 미검증이며
  실제 Gemini·유료 데이터 호출은 수행하지 않았다.

### 커밋 메시지 한글 작성 규칙 추가

- `AGENTS.md`에 저장소 커밋 메시지의 제목과 본문을 한글로 작성하도록 규칙을 추가했다.
- 코드 심볼·경로·API 이름 등 고유명사는 의미 보존을 위해 원문 사용을 허용한다.

### 종목 분석실 숫자 반응형·부호 표시 개선

- `/research/lab` 결과 카드와 지표 카드의 숫자가 모바일 폭에서 잘리지 않도록 카드 최소폭,
  글자 크기, 줄바꿈과 진입 구간의 값 배치를 조정했다.
- 수익률·52주 고점 대비·최대 낙폭·차트 당일 수익률은 양수에 `+`/민트색, 음수에 `−`/빨간색,
  0에 중립색을 적용했다. 방향성이 아닌 일반 지표에는 색으로 투자 판단을 암시하지 않는다.
- `PER · 실적`은 양수 TTM/연간 PER, `PER · 예상`은 Yahoo의 양수 예상 PER이 있어야 보이며,
  적자·0 EPS·미제공 값은 오해 방지를 위해 `자료 부족`으로 유지한다.
- PER 결측 사유를 EPS·순이익·시가총액·Yahoo 예상 비율 미제공 등 구체적인 문구로 지표 카드에
  표시하고 관련 회귀 테스트를 추가했다.
- 검증: 390px 미리보기에서 가로 넘침 없이 카드·지표·가치평가 입력 영역을 확인했고,
  전체 pytest **281 passed, 1 skipped**, 전체 Ruff와 `git diff --check`를 통과했다.

### GDELT 검색 제한 대응

- GDELT DOC API의 실제 HTTP 429 응답(“5초에 1회 요청 제한”)을 재현해 원인을 확인했다.
- 검색 상한을 최종 반영 한도인 12건으로 낮추고 요청 타임아웃을 15초, 전체 데드라인을 24초로
  조정했다. HTTP 429는 5초 대기 후 한 번 재시도하며, 재차 거절되면 공급자 요청 빈도 제한
  사유를 화면 상태에 표시한다.
- 공용 GDELT IP 제한은 외부 상태라 애플리케이션이 보장할 수 없으므로 DB·Yahoo·공시 기반
  분석은 계속 진행한다. 관련 어댑터·429 테스트를 추가했다.

### 시니어 트레이더 에이전트·분석 로직 검증 스킬 추가

- `docs/agents/senior_trader.md`와 `docs/agents/prompts/senior_trader.md`에 주식·증권 시니어
  트레이더 역할, 호출 규칙, 리스크·기준일·반대 시나리오 검토 원칙을 추가했다.
- `/Users/wonjjong/.codex/skills/stock-analysis-review/SKILL.md`에 종목 분석 로직 변경 시
  지표 정의·계산·결측·미래 정보 누출·편향·증권 실무 근거를 검증하는 재사용 스킬을 만들었다.
  `agents/openai.yaml`의 한국어 표시명과 기본 호출 문구도 설정했다.
- 두 산출물 모두 투자 확정 표현을 금지하고, 방법론의 일반적 사용과 특정 증권사 내부 모델을
  구분한다. 코드 변경은 도메인 문서·테스트 갱신과 함께 진행하도록 명시했다.

### 일반 프롬프트 Lead 자동 조정 추가

- `/Users/wonjjong/.codex/skills/lead-agent/SKILL.md`를 추가해 일반 저장소 변경 프롬프트도
  Lead 흐름으로 자동 분류하고 Backend·Frontend·QA·시니어 트레이더 역할을 필요한 범위에서
  적용하도록 했다.
- Lead Skill은 `AGENTS.md`, 도메인 문서, API 계약, 문서화·전체 검증 규칙을 우선 확인하고,
  협업 에이전트 사용이 허용될 때만 역할을 위임한다. 역할 위임이 불가능하면 단일 에이전트가
  역할별 검토를 순서대로 수행한다.
- `docs/agents/prompts/lead.md`에 `{TASK}`가 자동 문법이 아니라 명시적 위임용 참고 템플릿임을
  명시했다. 일반 프롬프트에서는 파일을 수정하거나 자리표시자를 치환하지 않는다.

## 2026-09-08

### 종목별 실시간 뉴스 검색 통합

- 요청 시 한국 Naver/미국 GDELT 검색과 Yahoo·DB 뉴스를 통합하고 최근 30일 중 관련 기사
  최대 12건을 뉴스 점수와 AI 근거에 함께 반영한다. 기존 DB의 분석된 중복 기사를 우선한다.
- `research/stock_news.py`, `services/news_search.py`, `services/stock_news.py`,
  `services/stock_analysis.py`를 추가해 순수 선별 규칙, 외부 검색 포트, ORM·캐시,
  HTML/JSON 공통 분석 흐름을 구분했다. View의 별도 뉴스 근거 조립은 공통 흐름으로 대체했다.
- 시장 데이터, 프롬프트, 분석 템플릿·미리보기, 설정·`.env.example`을 수정했다.
  Naver/GDELT와 Yahoo 검색 결과는 20분 캐시하며 DB 저장이나 마이그레이션은 추가하지 않는다.
  Yahoo 뉴스 장애가 시세 분석 전체를 막지 않도록 변경했다.
- 화면에 공급자별 조회·채택 건수, 실패·미설정·캐시 상태를 추가했다. JSON 응답은
  `newsSearch`, 보고서는 `dataGaps`를 추가한다. 대상 뉴스 점수를 갱신하지만 기존 후보의
  가격·재무 입력은 유지하며 다종목 랭킹 자체에 외부 검색은 추가하지 않는다.
- 국내 검색 활성화는 `SIGNALIST_NAVER_CLIENT_ID`/`SIGNALIST_NAVER_CLIENT_SECRET` 설정과
  서버 재시작이 필요하다. 키가 없으면 기존 데이터로 분석한다. 검색 실패는 재시도할 수 있게
  캐시하지 않는다. 원문은 요청하지 않으며 GDELT의 최초 수집 시각을 발행일로 단정하지 않는다.
- 관련 문서: `docs/domains/research/README.md`, `docs/policies/data-and-security.md`, 루트 README.
  상세 검색 제한, 점수 규칙, API 호환성 및 시각 의미를 문서화했다.
- 검증: `backend/`에서 `.venv/bin/python -m pytest` **222 passed, 1 skipped**,
  `.venv/bin/ruff check .` **통과**. 신규 도메인·HTTP MockTransport·통합 테스트 24개 포함.
  테스트는 외부 API/유료 AI를 호출하지 않는다. 실제 Naver 계정 키 연동 검증은 별도다.
- 한계: 제목·요약 규칙 분석은 본문 문맥·동명이인을 완벽히 판별하지 못한다. 검색 결과는
  전수 뉴스가 아니며 GDELT는 제목만 제공한다. JSON 분석도 대상 시장 조회를 사용하므로
  시세 조회 실패는 502로 반환한다.

## 2026-09-07

### AI 작업 문서화 의무화

- 파일 생성·수정·삭제를 포함한 모든 저장소 변경에서 관련 Markdown 문서를 같은 작업 안에
  갱신하도록 `AGENTS.md` 작업 절차와 완료 조건을 강화했다.
- 특정 도메인 변경은 `docs/domains/<도메인>/README.md`, 공통·플랫폼 변경과 시간순 작업
  이력은 이 문서에 기록하도록 역할을 구분했다.
- 기록 항목에 목적, 대상 파일, 동작·계약, 설계 판단, 외부 의존성, 마이그레이션·운영 영향,
  검증 결과, 한계와 삭제 영향까지 포함하도록 했다.

### 리서치 분석 화면과 데이터 계약 확장

- 한국 DART와 미국 SEC 공시 재무를 공통 펀더멘털 모델로 변환해 종목 분석 팩터에 반영했다.
- 뉴스가 없는 초기 운영 상태를 명시하고, 다종목 랭킹에서 뉴스 커버리지가 60% 미만이면
  뉴스 가중치를 제외하도록 했다.
- 단일 종목 분석에 ATR 기반 진입 구간·손절·목표 참고가, 20·60·100일 이동평균과 배열,
  최근 120거래일 캔들 차트를 추가했다.
- AI·시장·공시 호출 없이 화면을 확인하는 `/research/lab/preview` 샘플 화면과 분석 중 로딩
  상태를 추가했다.
- 외부 LLM의 408·5xx·네트워크 오류는 1초·2초 간격으로 같은 공급자에 재시도한 뒤에만
  쿨다운과 페일오버를 적용하도록 변경했다.
- 검증: `backend/.venv/bin/ruff check .`, `backend/.venv/bin/python -m pytest`
  (`198 passed, 1 skipped`).

### 현재 한계

- 증권사 컨센서스 목표가는 아직 수집하지 않는다. 국내는 FnGuide 같은 별도 계약형 데이터
  공급자가 필요하며, 현재 목표 참고가는 ATR과 거시 위험 강도로 계산한 기계적 기준이다.
