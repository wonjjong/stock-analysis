# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

.DS_Store
*.pem

# env files (can opt-in for committing if needed)
.env*
!.env.example

/outputs/
/work/
/data/

# Python / Django
__pycache__/
*.py[cod]
.venv/
venv/
*.sqlite3
db.sqlite3
local_settings.py
.mypy_cache/
.pytest_cache/
.ruff_cache/
staticfiles/
media/

# DB 덤프 — llm_providers 에 API 키가 들어 있다
*.dump
*.sql.gz

# 동일성 검증용으로 내려받은 실제 언론사 페이지 원문. 전문을 저장소에 두지 않는다
# (README 데이터 수집 원칙과 같은 이유). 이 파일들을 만들던 harvest-parity.mjs 는
# TypeScript 와 함께 삭제됐고, 지금은 재생성할 수 없다 — 자세한 것은
# test_parity_html.py 의 test_live_feed_fixture_matches 주석 참고.
backend/news/tests/parity/pages/

# Python 패키지 빌드 산출물
*.egg-info/

# 편집기·IDE 설정과 OS 부산물. 개발자마다 다르므로 추적하지 않는다.
.idea/
.vscode/
*.code-workspace
*.swp
*.swo
*~
.DS_Store?
._*
Thumbs.db
desktop.ini
