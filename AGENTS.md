# AGENTS.md

## 목적

이 저장소의 코드는 객체지향 설계와 DDD 원칙을 우선한다. 다만 원칙을 적용하기 위해
불필요하게 전체 구조를 재작성하지 말고, 변경 대상의 범위 안에서 점진적으로 개선한다.

## 설계 원칙

- 비즈니스 규칙은 Controller, View, Form, ORM Model, Repository 구현체에 흩어 놓지 않는다.
- 도메인 개념과 불변식은 Entity, Value Object, Aggregate 또는 순수한 도메인 모듈에 둔다.
- 객체의 상태 변경은 객체가 자신의 불변식을 검증한 뒤 수행한다.
- Value Object는 생성 시 유효성을 검증하고 가능하면 불변으로 만든다.
- Aggregate의 상태 변경은 Aggregate Root를 통해서만 수행한다.
- 도메인 계층은 Django, ORM, PostgreSQL, HTTP 클라이언트, 외부 AI SDK에 의존하지 않는다.
- 애플리케이션 계층은 유스케이스의 흐름, 트랜잭션, 포트 호출을 조정하고 도메인 규칙을 대신 구현하지 않는다.
- 외부 시스템과 데이터베이스 연동은 포트와 어댑터 경계 뒤에 둔다.
- Repository 인터페이스는 도메인 또는 애플리케이션이 필요로 하는 형태로 정의하고,
  구체적인 저장 기술은 구현체에 격리한다.
- 도메인 객체가 HTTP Request/Response, Django ORM 객체, 외부 API 응답 형식을 직접 알지 않게 한다.
- 문자열·숫자·불리언으로 표현된 중요한 비즈니스 개념은 적절한 타입이나 Value Object로 표현한다.
- Anemic Domain Model, God Object, God Service, Primitive Obsession, 무분별한 `utils`/`helpers` 모듈을 피한다.
- 추상화는 테스트를 위한 형식적 계층이 아니라 실제 변화 지점이나 외부 의존성을 격리할 때만 추가한다.
- 클래스와 메서드 이름은 프로젝트의 유비쿼터스 언어를 따르고, 기존 용어와 의미를 임의로 바꾸지 않는다.

## 현재 코드베이스의 경계

- `backend/news/crawler/`는 가능한 한 Django를 import하지 않는 순수 파싱·분석·도메인 로직으로 유지한다.
- `backend/news/services/`는 DB·네트워크와 유스케이스 조정을 담당한다. 핵심 비즈니스 규칙을 이곳에 무분별하게 넣지 않는다.
- `backend/news/views.py`, `backend/news/forms.py`, `backend/news/admin.py`는 입출력 변환과 호출 조정에 집중한다.
- `backend/research/services/`의 외부 데이터 조회 코드는 어댑터로 취급하고,
  외부 응답 모델을 내부 도메인 모델과 분리한다.
- 새 기능을 추가할 때 기존 구조를 존중하되, 도메인 규칙이 프레임워크 코드에 새로 침투하지 않게 한다.

## 작업 절차

1. 변경 전에 관련 코드, 테스트, README와 설정을 읽고 현재 경계를 파악한다.
2. 관련 유스케이스, 도메인 개념, Aggregate, 불변식, 외부 의존성을 식별한다.
3. 구현 전에 변경 방향, 중요한 설계 판단, 갱신할 문서 위치를 짧게 정리한다.
4. 가장 작은 범위로 구현하고, 무관한 리팩터링은 함께 수행하지 않는다.
5. 도메인 규칙은 프레임워크 없이 검증할 수 있는 테스트를 우선 작성한다.
6. 외부 연동과 저장소 코드는 필요한 경우 통합 테스트로 검증한다.
7. 기존 테스트를 삭제하거나 약화하지 않는다. parity 테스트가 있는 코드는 기존 동작과의 동일성도 유지한다.
8. 아래 작업 문서화 규칙에 따라 같은 변경 안에서 관련 Markdown 문서를 갱신한다.
9. 완료 전에 테스트와 린트를 실행하고, 실패하면 원인을 해결한 뒤 보고한다.

## 작업 문서화 규칙

AI 에이전트가 파일을 생성·수정·삭제하는 모든 작업은 코드만 바꾸고 끝내지 않는다. 코드,
테스트, 설정, 마이그레이션, 템플릿, 정적 파일, 스크립트, 문서를 포함한 저장소 변경은 같은
작업 안에서 관련 Markdown 문서에 상세히 반영한다. 읽기·분석·설명만 수행해 파일 변경이
없는 작업은 기록 대상에서 제외한다.

### 기록 위치

- 특정 도메인에 속한 변경은 `docs/domains/<도메인>/README.md`에 현재 동작과 계약을
  반영한다. 현재 도메인은 `news`, `research`이며 새 도메인이라면 해당 디렉터리와
  `README.md`를 만든다.
- 둘 이상의 도메인에 걸치거나 플랫폼, 빌드, 설정, 저장소 공통 규칙에 해당하는 변경은
  프로젝트 최상위 `CHANGELOG.md`에 기록한다.
- 중요한 도메인 변경은 도메인 README의 현재 상태를 갱신하는 것과 별개로 루트
  `CHANGELOG.md`에도 날짜별 작업 이력을 남긴다.
- 기존 문서의 목적과 맞지 않는 내용을 억지로 넣지 않는다. 현재 상태·계약은 도메인
  README에, 시간순 작업 이력은 `CHANGELOG.md`에 둔다.

### 필수 기록 내용

변경 성격에 맞게 다음 항목을 빠뜨리지 않는다.

- 작업 날짜와 작업 목적 또는 사용자 요구
- 생성·수정·삭제한 기능과 주요 파일
- 변경 전후의 사용자 관찰 동작과 API·도메인 계약
- 중요한 설계 판단, 불변식, 계산식 및 외부 의존성
- DB 마이그레이션, 환경변수, 배포·운영 절차와 호환성 영향
- 실행한 테스트·린트·검증 명령과 결과
- 남은 한계, 데이터 공백, 후속 작업이 필요한 사항
- 삭제 작업이라면 삭제 이유, 영향 범위, 대체 수단과 복구 가능 여부

### 완료 조건

- 구현과 문서가 서로 다르면 작업은 완료되지 않은 것으로 본다.
- 최종 보고에는 갱신한 문서 위치와 검증 결과를 포함한다.
- 문서만 수정하는 작업은 그 문서 자체가 변경 내용을 설명하면 중복 이력을 만들지 않아도
  되지만, 저장소 공통 정책 변경은 `CHANGELOG.md`에 한 번 기록한다.

## 커밋 메시지 규칙

- 저장소의 커밋 메시지는 제목과 본문을 포함해 한글로 작성한다.
- 코드 심볼, 파일 경로, API 이름처럼 한글로 바꾸면 의미가 흐려지는 고유명사는 원문을 유지할 수 있다.
- 커밋 제목은 변경 목적이 드러나는 명령형 또는 완료형의 짧은 문장으로 작성한다.

## 역할별 에이전트 지침

역할을 지정받은 에이전트는 작업 시작 전에 해당 지침을 읽는다.

- Backend: `docs/agents/backend.md`
- Frontend: `docs/agents/frontend.md`
- QA: `docs/agents/qa.md`

각 역할에 전달할 기본 프롬프트는 `docs/agents/prompts/`에 있다. Lead 에이전트는
작업을 역할별로 분해하고, 같은 파일을 여러 에이전트가 동시에 수정하지 않도록 조정한다.
Backend와 Frontend가 함께 변경될 때는 API 입력·출력·오류 계약을 먼저 확정한다.
QA는 구현이 끝난 뒤 독립적으로 검증하며, 실패 시 재현 절차와 기대 결과를 남긴다.

## 금지 사항

- 요구사항에 없는 도메인 규칙을 추측해서 추가하지 않는다.
- 비즈니스 로직을 View나 Service 하나에 몰아넣지 않는다.
- 단순 CRUD라는 이유로 도메인 불변식과 상태 전이를 우회하지 않는다.
- ORM 모델을 그대로 도메인 모델로 사용하거나, 외부 API 응답을 애플리케이션 전체에 전파하지 않는다.
- 기존 테스트를 통과시키기 위해 검증을 제거하거나 예외를 무시하지 않는다.
- 새로운 패키지·레이어·추상화를 추가할 때는 해당 경계가 필요한 이유를 설명한다.

## 검증 명령

프로젝트 루트가 아닌 `backend/`에서 실행한다.

```bash
cd backend
.venv/bin/python -m pytest
.venv/bin/ruff check .
```

변경 범위가 작더라도 관련 테스트를 먼저 실행하고, 완료 전에는 위의 전체 검증을 실행한다.
